# 业务逻辑漏洞梳理

> 面向 NMS（网管）/ 华为 ICT PDD 产品的代码安全审计。本文给出逻辑漏洞的严谨定义、与其他漏洞类的正确关系，并以 CWE-1000（Research View）的十大 Pillar 为骨架做有依据的分类，再用 OWASP 两个 Top 10 交叉验证。

## 一、纠正：逻辑漏洞不是和内存 / 注入并列的「第三类」

漏洞要分两条**正交的轴**来看，常见错误是把它们压成一条：

- **表现 / 影响轴（manifestation）**：漏洞最终如何出错 —— 内存破坏、注入 / RCE、信息泄露、DoS、越权。
- **缺陷本质 / 根因轴（root cause）**：为什么会出错 —— 程序的**推理、判断、计算、时序、状态、信任决策**错了。

「逻辑漏洞」是**根因轴上的描述**，不是表现轴上的一个并列项。所以它**会贯穿并引发**内存安全和注入类问题，而不是与之分开。CWE 用 **Chain（链）** 和 **Composite（复合）** 关系把这件事说清楚：一个逻辑层的弱点常常是**主弱点（primary）**，下游才表现为内存或注入。

几个典型的「逻辑根因 → 内存 / 注入表现」的 CWE 链：

| 逻辑根因（主弱点） | 下游表现（次弱点） | 说明 |
|---|---|---|
| CWE-193 Off-by-one（算错边界） | CWE-787 越界写 | 内存破坏的根是一个计算逻辑错误 |
| CWE-190 整数溢出 → CWE-131 缓冲区大小算错 | CWE-787 / CWE-125 | 经典的「算错长度」链 |
| CWE-20 输入校验不当 / CWE-807 用不可信输入做决策 | CWE-89 SQLi / CWE-78 命令注入 | 注入能到达 sink，本质是「该不信任却信任」的逻辑判断缺失 |
| CWE-178 大小写处理逻辑错 | CWE-287 认证绕过 | 比较逻辑写错导致鉴权被绕 |
| CWE-665 初始化逻辑缺失 | CWE-200 信息泄露 / CWE-457 用未初始化值 | 状态管理逻辑错导致泄露 |

**结论**：内存安全（CWE-664 家族）和注入（CWE-707 家族）很多时候**其根因就是逻辑漏洞**。判断一个漏洞是不是「逻辑类」，看它的**根因是不是程序逻辑（计算 / 比较 / 时序 / 状态 / 信任 / 缺失的检查）**，而不是看它最终炸在哪里。

同时要区分两种用法：

- **狭义「业务逻辑漏洞」**（OWASP A04 Insecure Design，CWE-840 / 841）：违反应用特定的业务规则，没有通用特征签名。
- **广义「逻辑漏洞」**：程序推理层面的一切缺陷，包括那些导致内存 / 注入的计算、比较、时序错误。

## 二、有依据的分类：四套分类法 + 以 CWE-1000 Pillar 为骨架

### 2.0 为什么用四套分类法交叉，而不是一套（分类依据的依据）

逻辑漏洞没有「唯一正确」的分类——因为**不同分类法是沿不同的轴切的**。四套权威来源各有其组织原则与证据基础，互为印证：

| 分类法 | 组织轴（依据） | 证据基础 | 角色 |
|---|---|---|---|
| **CWE-1000 Pillar** | **根因轴**：缺陷的本质机理（计算/比较/时序/状态/信任/缺失检查） | MITRE 社区共识、可追溯的 Chain/Composite 关系图谱 | **骨架**（最稳定、最权威的「大类」出处） |
| **OWASP API Security Top 10 (2023)** | **攻击面 × 授权粒度轴**：API 上攻击者打哪一层 | 工业界 API 攻防数据、采用度最高 | 访问控制类的**粒度细分**（BOLA/BOPLA/BFLA） |
| **OWASP WSTG-BUSL** | **测试方法轴**：渗透测试员手工怎么测 | OWASP 测试方法论手册，9 个标准化用例 | **可执行的测试流程**（无工具签名，需理解意图） |
| **OWASP Top 10 for Business Logic Abuse (2025)** | **图灵机模型轴**：Tape/State/Transition 三类，看缺陷"住"在哪 | **数据驱动**：LLM 对 2023–2025 约 7.6 万条 CVE 聚类 + 2.5 万 GitHub 安全 issue 校验 | 逻辑滥用的**细分词表**（最新、最聚焦逻辑） |

**关键洞察**：四套法切的轴不同，但**落点高度重合**——同一个漏洞在 CWE 里是 CWE-841，在 API 里是 API6，在 WSTG 里是 BUSL-06，在 BLA 里是 BLA6(MTV)。这种**跨四套法的收敛**本身就是分类正确性的最强证据：不是某个人的主观划分，而是四个独立视角指向同一组结构。所以本文**以 CWE Pillar 为骨架**（根因最稳），把另外三套作为**证据与细分**挂在每个 Pillar 下。

### 2.1 骨架：CWE-1000 的十大 Pillar

CWE Research View（CWE-1000）的**最顶层只有 10 个 Pillar**，这是最权威的「大类」出处。先列全 10 个，再标注哪些承载逻辑缺陷，最后逐个下分。

| Pillar | 名称 | 是否承载逻辑缺陷 |
|---|---|---|
| CWE-284 | Improper Access Control（访问控制不当） | ✅ 逻辑核心 |
| CWE-682 | Incorrect Calculation（计算错误） | ✅ 逻辑核心（也常引发内存） |
| CWE-691 | Insufficient Control Flow Management（控制流管理不足） | ✅ 逻辑核心 |
| CWE-693 | Protection Mechanism Failure（保护机制失效） | ✅ 逻辑核心 |
| CWE-697 | Incorrect Comparison（比较错误） | ✅ 逻辑核心 |
| CWE-703 | Improper Check/Handling of Exceptional Conditions（异常处理不当） | ✅ 逻辑核心 |
| CWE-435 | Improper Interaction Between Multiple Entities（组件交互不当） | ✅ 组合逻辑 |
| CWE-664 | Improper Control of a Resource Through its Lifetime（资源生命周期控制不当） | ◐ 含内存安全，常被逻辑链引发 |
| CWE-707 | Improper Neutralization（中和不当＝注入） | ◐ 注入，常被逻辑链引发 |
| CWE-710 | Improper Adherence to Coding Standards（编码规范） | △ 元类，非逻辑专属 |

逻辑漏洞**集中在前 7 个 Pillar**，并通过 Chain **下穿到 664（内存）和 707（注入）**。

### 2.2 另三套分类法速览（供下文每个大类挂接）

**(a) OWASP API Security Top 10（2023）—— 授权粒度三兄弟**，全部根因落在 Pillar CWE-284：

| 风险 | 越权粒度 | 一句话 | CWE |
|---|---|---|---|
| **BOLA** (API1) | 对象（哪条记录） | 这个**对象**是你的吗 | CWE-639 / 566 |
| **BOPLA** (API3) | 属性（对象里的字段，含读越权 Excessive Data Exposure + 写越权 Mass Assignment） | 这个**字段**你能读/写吗 | CWE-915 / 213 |
| **BFLA** (API5) | 功能（操作/端点，垂直越权） | 这个**操作**你的角色能做吗 | CWE-862 / 863 |

**(b) OWASP WSTG-BUSL（业务逻辑测试，9 用例）—— 手工测试流程**，主要印证 Pillar CWE-691/693/682：BUSL-01 业务数据校验、02 伪造请求、03 完整性校验、04 时序(竞态/侧信道)、05 限次动作、06 工作流绕过、07 滥用防御、08/09 非预期/恶意文件上传。其纲领是**无自动化签名，必须先逆向"应保证的不变量"再尝试违反**。

**(c) OWASP Top 10 for Business Logic Abuse（2025）—— 图灵机模型 + CVE 数据驱动**。把应用建模为图灵机，按缺陷所在的原语分三组：**Tape Issues**（数据存储：不一致/损坏/越权改）、**State Issues**（状态：初始化/状态管理不当）、**Transition Issues**（转移：流程绕过/竞态/弱校验）。十大类：

| BLA 编号 | 名称 | 图灵机归属 | 主映射 Pillar |
|---|---|---|---|
| BLA1 | Action Limit Overrun (ALO) 动作越限 | Transition | CWE-691 (837) |
| BLA2 | Concurrent Workflow Order Bypass (CWOB) 并发顺序绕过 | Transition | CWE-691 (362/367) |
| BLA3 | Object State Manipulations (OSM) 对象状态篡改 | State | CWE-691 (841) |
| BLA4 | Malicious Logic Loop (MLL) 恶意逻辑循环 | Transition | CWE-691/834 |
| BLA5 | Artifact Lifetime Exploitation (ALE) 工件生命周期利用 | Tape | CWE-664 (613/384) |
| BLA6 | Missing Transition Validation (MTV) 缺转移校验 | Transition | CWE-691 (841) |
| BLA7 | Resource Quota Violation (RQV) 配额违规 | Tape/State | CWE-693 (770/799) |
| BLA8 | Internal State Disclosure (ISD) 内部状态泄露 | Tape | CWE-664/200 |
| BLA9 | Broken Access Control (BAC) 访问控制失效 | Head/State | CWE-284 |
| BLA10 | Shadow Function Abuse (SFA) 影子功能滥用 | Transition | CWE-284/691 |

> 注：BLA 是 2025 新项目，编号/命名仍可能微调；图灵机建模是其卖点亦是争议点（其优势是**数据驱动**而非专家拍脑袋）。

### 2.3 逐个下分（每个大类挂接四套法 + CWE 编号）


### 大类 1：访问控制不当（CWE-284）—— 逻辑漏洞占比最大
- **对象级越权 / IDOR**：CWE-639 用户可控键绕过授权、CWE-566 主键越权 → 对应 **OWASP API1 BOLA**
- **功能级越权**：CWE-862 缺失授权、CWE-863 错误授权 → **OWASP API5**
- **属性级越权 / Mass Assignment**：CWE-915 → **OWASP API3**
- **缺失关键功能认证**：CWE-306
- **权限管理错误**：CWE-269 提权、CWE-266 错误赋权、CWE-250 过高权限运行、CWE-732 关键资源权限错配
- **授权粒度不足**：CWE-1220
- **资源暴露到错误域**：CWE-668、CWE-22 路径穿越（注意：路径穿越根因是访问控制逻辑）

### 大类 2：计算错误（CWE-682）—— 逻辑，且最常引发内存
- 整数溢出 / 下溢：CWE-190 / CWE-191（→ 链到 CWE-787 内存）
- Off-by-one：CWE-193（→ 链到越界）
- 缓冲区大小算错：CWE-131（→ 链到越界）
- 精度 / 舍入误差：CWE-1339（金融套利）、除零 CWE-369
- **业务计算错误**：价格 / 金额 / 数量 / 折扣的计算逻辑（属狭义业务逻辑）

### 大类 3：控制流管理不足（CWE-691）—— 时序与状态机逻辑
- 竞态：CWE-362 Race Condition、CWE-367 TOCTOU、CWE-364 信号处理竞态
- 同步缺陷：CWE-662、死锁 CWE-833、不受控递归 CWE-674
- **行为顺序错误**：CWE-696（流程乱序）
- **工作流执行不当**：CWE-841（步骤跳过 / 非法状态转移，**狭义业务逻辑核心**）
- **单次动作未强制唯一**：CWE-837（重放、重复提交、重复领取）

### 大类 4：保护机制失效（CWE-693）
- **客户端强制服务端安全**：CWE-602（前端校验后端不校）
- **用不可信输入做安全决策**：CWE-807（信任 Host / Referer / X-Forwarded-For）
- **信任边界违规**：CWE-501
- **频率控制不当**：CWE-799（限流缺失，使暴破 / 枚举可行）→ 也关联资源滥用
- **业务逻辑错误总类**：CWE-840（显式的「业务逻辑」类别）
- 对应 **OWASP A04 Insecure Design**

### 大类 5：比较错误（CWE-697）
- 允许列表过宽 CWE-183 / 拒绝列表不全 CWE-184
- 大小写 / 规范化处理错 CWE-178、CWE-289
- 引用比较 vs 值比较 CWE-595 / 597、按类名比较 CWE-486、不完整比较 CWE-1023
- 这些常下穿到**认证绕过（CWE-287）**

### 大类 6：异常处理不当（CWE-703）—— 「失败开放」逻辑
- **未安全失败 / Fail-Open**：CWE-636（鉴权异常时放行）
- 异常条件检查缺失：CWE-754、异常处理不当：CWE-755
- 返回值未检查：CWE-252、检测到错误却无动作：CWE-390、未捕获异常：CWE-248

### 大类 7：组件交互不当（CWE-435）
- API 间不安全消费：**OWASP API10**
- SSRF：CWE-918（信任「内网地址一定安全」的假设）→ **OWASP A10 / API7**
- 多组件授权模型不一致（南北向鉴权不一致）

### （下穿）大类 8：资源生命周期（CWE-664）与 大类 9：注入（CWE-707）
- 这两类的内存安全（CWE-787 / 125 / 416 / 415 / 476）和注入（CWE-89 / 78 / 79 / 94 / 611）**本身多由前 7 类的逻辑链引发**，故不另起独立「逻辑分类」，而是作为逻辑链的**下游表现**统一收录。
- 但其中**生命周期 / 状态**部分本就是逻辑：对象释放后操作 CWE-672、初始化不当 CWE-665、会话过期不足 CWE-613、会话固定 CWE-384。

## 三、OWASP 视角交叉验证（从「攻击者利用什么」反向印证）

同一套结构，从 OWASP 两个 Top 10 映回 Pillar，验证分类自洽：

| OWASP 项 | 映射 Pillar | 性质 |
|---|---|---|
| A01 Broken Access Control / API1 BOLA / API5 / API3 | CWE-284 | 逻辑 |
| A04 Insecure Design / API6 业务流滥用 | CWE-693 / 691 / 840 / 841 | 狭义业务逻辑 |
| A07 认证失败 / API2 | CWE-287 / 384 / 613 / 307 / 640 | 逻辑 |
| A08 软件与数据完整性失效 | CWE-345 / 494 / 502 | 逻辑（信任）+ 反序列化可下穿 RCE |
| API4 资源消耗不受限 | CWE-770 / 799 | 逻辑（限额） |
| A10 / API7 SSRF | CWE-918 / CWE-435 | 逻辑（信任假设） |
| A03 Injection | CWE-707 | **表现层**，根因常在 CWE-20 / 807（逻辑） |
| A09 日志监控失效 | CWE-778 | 逻辑（可审计性） |

注意 A03 注入在 OWASP 里是独立项，但其**根因**落在逻辑（输入信任决策）—— 这正好佐证第一节：表现轴和根因轴是两回事。

## 四、对 NMS / 华为 ICT PDD 审计的取舍

按上面的 Pillar 骨架，audit-engine 探针应**按根因轴的前 7 个 Pillar 建主探针**，而非按表现分：

- **CWE-284 主战场**：多租户 / 多区域隔离（BOLA）、网元纳管授权边界、NBI 与南向鉴权一致性、Job/Task IDOR、批量接口逐项漏校验。
- **CWE-691**：配置下发状态机（未审批即激活、回滚非法转移）、并发竞态突破单次约束。
- **CWE-693 / CWE-703**：鉴权服务异常 fail-open、限流缺失、客户端强制。
- **下穿监控**：当 CWE-682（长度 / 大小计算）、CWE-20（输入信任）命中时，顺链检查是否引发 664（内存）/ 707（注入）—— 这恰好让一个「逻辑探针」同时覆盖内存和注入根因，而不是分三套引擎。

这与 business-flow 粒度结论一致：**逻辑探针沿 Pillar 走、顺 Chain 下穿**，正好把内存 / 注入纳入逻辑根因视角统一捕捉。具体探针清单见同目录 `audit-engine/templates/checklist.md`（已按 7 Pillar 展开为 CK1–CK9，含「应保证的不变量 + 触发条件 + 典型代码征兆 + 关联 CWE/OWASP」）。
